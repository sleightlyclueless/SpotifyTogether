// GET list of all event tracks
// - return all event tracks

// GET ids of all playlists ???
// - empty map of ids
// - iterate all event tracks:
//   - add id to map

// GET all tracks of playlist
// - iterate all Event Tracks
//   - if playlist[] contains playlistID
//   - add EventTrack



// POST propose new event track
// - fetch spotify data, create spotify track
// - create and add event track, set proposed

// PUT change event track status
// - denied: set
// - proposed: return;
// - accepted_playlist: -> set
// - generated: -> return;
// - accepted: -> set



// POST propose playlist
// [- check if playlists exists -> remove]
// - fetch data from spotify
// - iterate all tracks:
//   - if event track exists -> skip (continue)
//   - else add new event track as proposed & possibly spotify track

// PUT accept all tracks from playlist
// - iterate all event tracks:
//   - status generated/accepted: nothing
//   - status denied/accepted_playlist: nothing
//   - status proposed: set accepted_playlist

// DELETE remove playlist
// - iterate event tracks:
//   - status generated/accepted: keep track
//   - status denied: keep track (blacklist ?)
//   - status accepted_playlist/proposed: remove track
//     -> check if SpotifyTrack can be removed from db ?
// - remove playlist